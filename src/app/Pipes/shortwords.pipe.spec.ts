import { ShortwordsPipe } from './shortwords.pipe';

describe('ShortwordsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortwordsPipe();
    expect(pipe).toBeTruthy();
  });
});
